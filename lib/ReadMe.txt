Important things to NOTE:
1. run this command in your terminal "flutter build apk --release" //this is used to build and release and apk version of your project.
2. after building, you can locate this file in this directory 
= > your project folder => build => app => outputs => apk => release => app-release. 
// you can copy this and send them to friends to intall and check.


GIT THINGS
3. …or create a new repository on the command line
echo "# flutter-to-do" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/samueluzoma/flutter-to-do.git
git push -u origin main
4. You can commit to git by using the command = > git push -u origin main

5 …or push an existing repository from the command line
git remote add origin https://github.com/samueluzoma/flutter-to-do.git
git branch -M main
git push -u origin main 